# TirthConstruction
start by oppening cmd with tirthConstruction directory.
# write code if first time-
```
npm install
cd frontend
npm install
cd backend
npm install
```
if error comes " try in backend directory"
```
npm install mongoose
```
# To run project
Go on vs code and open tirthconstuction folder
in that open terminal
```
cd backend
node server.js
```
after that press ctrl+Shift+`(~) botton to add new terminal
```
cd frontend
npm run dev
```
# for git push and pull-
push
  - check in which branch u r by
```
git branch
```
  - to change branch or create branch
```
git checkout -b nameOfBranch
```
  - to push
```
git add .
git commit -m "Message"
git push origin branchNameInWhichUR
```
Pull
```
git pull origin main
```
